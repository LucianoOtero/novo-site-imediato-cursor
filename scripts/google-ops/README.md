# google-ops

Scripts OAuth + Tag Manager para o container `GTM-PD6J398`.

Documentação completa: [`docs/GTM_ADS_OAUTH_OPS.md`](../../docs/GTM_ADS_OAUTH_OPS.md).

```powershell
# 1) Coloque client_secret.json (Desktop OAuth) nesta pasta
npm install
npm run auth
npm run gtm:whoami
npm run gtm:inspect
```
