# dependencies
/node_modules
/.pnp
.pnp.js

# testing
/coverage

# next.js
/.next/
/out/

# production
/build

# misc
.DS_Store
*.pem

# debug
npm-debug.log*
yarn-debug.log*
yarn-error.log*

# local env files (nunca commitar valores reais)
.env
.env.local
.env.*.local

# Chaves de service account do Firebase (nunca commitar) — ver
# docs/ARQUITETURA_LEADS_FIREBASE_CLOUD_FUNCTION.md
firebase-*-key.json
firebase/functions/.env
firebase/functions/*-key.json
firebase/functions/node_modules
firebase/.firebase

# vercel
.vercel

# typescript
*.tsbuildinfo
next-env.d.ts

# Issue 12 — persistência local de leads (placeholder de dev; ver
# lib/leads/store.ts). Nunca deve ir para o repositório (dados de lead).
/.data

# Base local de testes RPA com PII real de renovação (gerada a partir do Excel).
# Regenerar com: python scripts/convert_renovacao_excel_to_testdata.py
/testdata/rpa/

# Backup/export antigo do projeto (48MB) — não é código-fonte, não deve
# ir para o repositório.
novo site imediato cursor.zip
*.zip

# Candidatas de imagem geradas via Higgsfield (rascunhos p/ aprovação) —
# só o asset aprovado/otimizado vai para public/hero/.
/public/v2-candidatas/

# Playwright (teste de fidelidade RPA) — browsers, relatórios e artefatos.
/e2e/playwright-report/
/e2e/test-results/
/test-results/
/playwright-report/
/playwright/.cache/
