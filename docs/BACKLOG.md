# BACKLOG

## Finalidade
Backlog executável do projeto, com todas as issues preparatórias e de implementação da Fase 1, em ordem de dependência, prontas para importação em uma ferramenta de rastreamento (GitHub Issues/Linear).

## Origem
Derivado da seção 8 (Backlog detalhado de issues) e da seção 8.2 (Ordem de execução recomendada, rev. 4.1) de `PLANO_IMPLEMENTACAO.md`. Extraído na Issue P-08.

## Status
CONTEÚDO CRIADO — Issue P-08 concluída. Sem acesso configurado a GitHub/Linear nesta sessão; backlog materializado como este arquivo, conforme previsto no próprio plano ("Arquivos: `docs/BACKLOG.md` e/ou exportação para a ferramenta").

## Observações
Este arquivo é um backlog **rastreável e resumido** (título, dependências, aceite/fora de escopo resumidos, status real de progresso). Para o objetivo completo, contexto da especificação, arquivos prováveis, passos detalhados e prompt recomendado de cada issue, consulte a seção 8 de `PLANO_IMPLEMENTACAO.md` — este arquivo não duplica esse conteúdo integralmente para evitar divergência entre fontes. A coluna **Status** reflete o progresso real desta execução (não é um dado inventado: reflete o que já foi concluído nesta sessão via Cursor).

---

## Issues preparatórias (Fase 0)

| ID | Título | Status | Depende de | Aceite (resumo) | Fora de escopo (resumo) |
|---|---|---|---|---|---|
| P-01 | Criar `/docs` | ✅ Concluída | Repositório criado | Todos os arquivos da seção 4 criados, cada um com finalidade/origem/status | Preencher conteúdo final |
| P-02 | Dividir especificação em arquivos menores | ✅ Concluída | P-01 | 9 docs "núcleo" com seções mapeadas, sem resumo/reinterpretação | Código; mudar decisões |
| P-03 | Criar `DECISIONS.md` (+ ADR-0009) | ✅ Concluída | P-01 | 9 ADRs datados (ou `TODO` justificado); 1–8 "Aceita"; ADR-0009 "Proposta" | Decisões novas não listadas |
| P-04 | Criar `.env.example` | ✅ Concluída | P-01 | Todas as vars da seção 45 com placeholders; nenhum segredo em `NEXT_PUBLIC_*` | `.env.local`; valores reais |
| P-05 | Criar `DADOS_OFICIAIS.md` | ✅ Concluída | P-01 | Tabela completa da seção 64; coluna Confirmado? fiel à fonte; CRÍTICO/RESOLVER preservados | Preencher dados não confirmados |
| P-06 | Criar `INVENTARIO_URLS.md` | ✅ Concluída | P-01 | Tabelas 59/65.1 + checklist 65.2; coluna Ação só com valores permitidos | Decidir ações sem dados de tráfego |
| P-07 | Criar `BASELINE_METRICS.md` | ✅ Concluída | P-01 | Tabela completa da seção 66; métricas Obrigatórias destacadas; tudo `TBD` | Coletar números reais |
| P-08 | Criar backlog (este arquivo) | ✅ Concluída | P-01..P-07 | Issues na ordem de dependência; cada uma com aceite/fora | Issues de fases 2+ |
| P-09 | Auditoria do JavaScript legado do Webflow | ✅ Concluída (parcial — ver limitações) | P-01; acesso Webflow/GTM | 5 tabelas em `LEGACY_JS_AUDIT.md`+correlatos; cada item Crítico?/Migrar? | Reimplementar comportamento (Issues 11/12/18/19) |
| P-10 | Auditoria e migração seletiva de SVGs/assets | ✅ Concluída parcialmente — auditoria via site publicado; pendente acesso Webflow Asset Manager/Cloudinary real, se necessário | P-01; acesso Webflow Asset Manager | 5 tabelas em `SVG_ASSETS_AUDIT.md`+correlatos; classificação por asset | Baixar/copiar assets sem classificação |

## Issues de implementação (Fase 1) — ordem de execução rev. 4.1

| # | ID | Título | Status | Depende de | Aceite (resumo) | Fora de escopo (resumo) |
|---|---|---|---|---|---|---|
| 1 | Issue 01 | Scaffold Next.js 15 + Tailwind 4 + shadcn/ui | ✅ Concluída | P-01..P-04 | Build limpo; typecheck/lint ok; shadcn inicializado | Componentes de negócio; conteúdo |
| 2 | Issue 02 | Tokens do Design System | ✅ Concluída | Issue 01 | Tokens utilizáveis; sem valor mágico; contraste AA | Aplicar em componentes |
| 3 | Issue 03 | Layout base, fontes, providers, GTM inicial | ✅ Concluída | Issues 01–02 | Fonts sem requisição externa; GTM carrega; consent default `denied` | Banner completo; integração real GTM/GA4 |
| 4 | Issue 03A | Configuração de ambiente e validação de env | ✅ Concluída | Issues 01–02 | `.env.example`/`lib/env.ts`; segredo nunca em `NEXT_PUBLIC_*`; falha clara no boot | Redirects/deploy (Issue 24) |
| 5 | Issue 03B | Analytics mínimo e contrato de eventos | ✅ Concluída | Issue 03A | `trackEvent()` tipado; não quebra sem `window`/`dataLayer`; sem PII | Integração real GTM (Issue 18) |
| 6 | Issue 04 | `lib/company.ts` | ✅ Concluída | P-05 | Tipo completo; nada hardcoded em JSX; pendências `A_CONFIRMAR` | Consumo nos componentes |
| 7 | Issue 05 | `lib/ramos.ts` | ✅ Concluída | Issue 04 | 10 ramos tipados; `getRamo(slug)`; preços da lib | Renderização |
| 8 | Issue 08 | Componentes base (Button, Section, Container, Badge, Chip) | ✅ Concluída | Issue 02 | Variantes via `cva`; foco visível; ≥44px | Componentes de negócio |
| 9 | Issue 06 | Header + Drawer mobile | ✅ Concluída (logo em texto — asset final de P-10 pendente) | Issues 02, 04, 08 (+P-10 para logo final) | Focus trap; Esc fecha; ≥44px; encolhe no scroll | Footer; conteúdo de páginas |
| 10 | Issue 07 | Footer | ✅ Concluída | Issues 04, 08 (+P-10 para logo final) | Zero hardcode; links legais corretos; acessível | Página de fraude (Issue 22) |
| 11 | Issue 09 | CredBar | ✅ Concluída | Issues 04, 08 | Números com contexto textual (SR); responsivo | Carrossel de reviews |
| 12 | Issue 10 | InsuranceCard + RamoGrid | ✅ Concluída | Issues 05, 08 (+P-10 para assets finais) | Preço "a partir de" da lib; hover lift | Página da Home |
| 13 | Issue 11 | LeadForm multi-step | ✅ Concluída | Issues 02, 08, 03B, P-09 | 2 campos viram lead; valida sem reload; eventos via 03B | `/api/lead` (Issue 12) |
| 14 | Issue 12 | `/api/lead` | ✅ Concluída (persistência em arquivo local — DB real pendente, ver `lib/leads/store.ts`) | Issues 03A, 04, 11, validators, P-09 | 201/422/200-dup/429; lead nunca perdido | UI; CRM real (mock via env) |
| 15 | Issue 13 | `/cotacao` | ✅ Concluída | Issues 11, 09 | Form funcional; metadata; mobile-first | — |
| 16 | Issue 14 | `/obrigado` | ✅ Concluída | Issues 11, 03B | Conversão dispara 1×; noindex; CTA WhatsApp | — |
| 17 | Issue 17 | FAQ + schema | ✅ Concluída | Issues 05, 08 | ARIA correto; Rich Results válido; `faq_open` dispara | — |
| 18 | Issue 19 | WhatsApp/call tracking + FAB + StickyCTA | ✅ Concluída | Issues 03A, 03B, 04, 05, P-09 | Msg pré-preenchida por ramo; eventos disparam | — |
| 19 | Issue 18 | GTM/dataLayer (completar integração real) | ⏳ Parcial — ver observação | Issues 03B, 03A, P-09; acesso GA4/GTM/Ads | Eventos no DebugView; sem PII em claro | Contrato mínimo (já na 03B) |
| 20 | Issue 20 | SEO metadata + JSON-LD | ✅ Concluída (schema com campos sem dado confirmado omitidos — ver `lib/schema.ts`) | Issues 04, 05, 17 + dados confirmados | Title/desc únicos; OG por página; Rich Results válido | Article/blog schema (Fase 3) |
| 21 | Issue 21 | sitemap + robots | ✅ Concluída (URLs de páginas ainda não construídas — Issues 15/16 — listadas conforme mapa do site aprovado, seção 4) | Issues 16, 20 | URLs corretas; lastmod; aponta sitemap | pSEO |
| 22 | Issue 22 | FraudAlert (banner + página) | ✅ Concluída (texto A_CONFIRMAR com Jurídico — não capturado literalmente pela auditoria P-09; banner inserido em `app/(marketing)/layout.tsx` em 2026-07-03, acima do Header, após a Home existir) | Issues 08, 04 | Dismiss persiste; página indexável; acessível | — |
| 23 | Issue 23A | Staging seguro e não indexável | ✅ Concluída | Issues 03A, 03 | Staging não indexável; banner visível; produção sem banner | Deploy de produção final (Issue 24) |
| 24 | Issue 23B | Auditoria anti-hardcode | ✅ Concluída | Issues 04, 05 | Padrões proibidos definidos; comando `check:hardcode`; PR bloqueado se detectar | Correção automática; lint de tokens |
| 25 | Issue 15 | Home | ✅ Concluída (ver nota) | Issues 02,04,05,08,09,10,11,17,19 + 23A + 23B | 1 H1; LCP no hero; reutiliza blocos prontos | Criar componentes do zero |
| 26 | Issue 16 | Template `/seguro-[ramo]` | ✅ Concluída (ver nota) | Issues 04,05,10,11,17,19,20 + 23A + 23B | 10 LPs geradas; sem copy duplicada manual | pSEO (cidade/modelo) |
| 27 | Issue 23 | QA mobile-first + a11y | ✅ Concluída (ver nota) | Páginas e componentes prontos | 0 violação a11y crítica; ≥44px; sem CLS | Storybook completo (Fase 2) |
| 28 | Issue 24 | Deploy + redirects + go-live | ⏳ Parcial — ver observação | Issues 23A, 23B, `INVENTARIO_URLS.md`, checklist comercial | 301s ativos; env de produção validado; build ok | Criação primária de `lib/env.ts` (03A) |
| 29 | Extensão pós-24 | Integrações externas do Webflow (EspoCRM/Octadesk/RPA/PH3A/Consent Banner/Search Console) | ✅ Concluída (ver nota) | Issue 24, `docs/WEBFLOW_CUSTOM_CODE_DEV.md` | Destinos reais de lead confirmados e implementados; RPA/PH3A opcionais e não-bloqueantes; banner de consentimento real; tags de verificação | Credenciais reais de produção; validação em staging |
| 30 | Extensão pós-24 | Inserção do `FraudAlert` no layout + confirmação de `DADOS_OFICIAIS.md` + páginas legais | ✅ Concluída (ver nota) | Issue 22, Issue 15 | Banner visível nas páginas de marketing; todos os dados institucionais/comerciais confirmados; `/politica-de-privacidade` e `/termos` publicadas | Revisão jurídica formal dos textos genéricos adotados |
| 31 | Extensão pós-24 | `InsurersGrid` — migração real dos 18 logos de seguradoras | ✅ **Concluída integralmente (2026-07-07)** — os 8 logos pesados foram vetorizados pelo freelancer e substituídos | Issue 15, Issue 16, `docs/BRAND_ASSETS.md` | 18 logos reais baixados e migrados, **todos vetores puros**; componente implementado e integrado na Home e nas 10 LPs de ramo | TeamStrip; Testimonials |
| 32 | Extensão pós-24 | `TeamStrip` + `/equipe` — migração real das 16 fotos da equipe | ✅ Concluída (ver nota) | Issue 15, `docs/IMAGE_ASSETS_INVENTORY.md` | 16 fotos reais baixadas e migradas, com confirmação do cliente de que a equipe/fotos seguem atuais; `TeamStrip` (resumo de 8) na Home e `/equipe` (grade completa) publicados | Cargo/função de cada colaborador (não fornecido) |
| 33 | Extensão pós-24 | `Testimonials` — carrossel Embla com avaliações reais do Google | ✅ **Concluída integralmente (2026-07-08)** — API do Google Places ativada e conectada de verdade | Issue 15, `embla-carousel-react` | Carrossel funcional (arrastar/setas/dots) com avaliações e nota/contagem reais ao vivo; fallback com 12 avaliações reais caso a API fique indisponível | — |
| 34 | Extensão pós-24 | QA/a11y focado nos blocos novos (InsurersGrid, Testimonials, TeamStrip, `/equipe`) | ✅ Concluída (ver nota) | Issues 31, 32, 33 | 0 violações a11y críticas; alvos de toque ≥44px nos controles primários; sem overflow horizontal em 360px | Auditoria completa nova (só cobriu o que mudou desde a Issue 23) |
| 35 | Extensão pós-24 | Rascunho de FAQ/objeções por ramo + coberturas faltantes em `lib/ramos.ts` | ✅ Concluída (ver nota) | Issue 05, Issue 16 | Rascunho genérico para os 10 ramos, sem inventar dados comerciais/regulatórios específicos; coberturas publicadas ao vivo (decisão do cliente), FAQ/objeções aguardando revisão | Revisão de Conteúdo/Comercial dos textos de FAQ/objeções |
| 36 | Extensão pós-24 | `ContactLeadModal` — réplica (com correção) do modal de captura de lead antes de WhatsApp/telefone do site legado | ✅ Concluída (ver nota) | `docs/LEGACY_JS_AUDIT.md`, Issue 11, Issue 12, Issue 19 | Modal com 8 campos (DDD/Celular obrigatórios) em todos os pontos de contato (WhatsAppButton, CallButton, WhatsAppFAB, StickyCTA, Footer); envio via `/api/lead` (EspoCRM+Octadesk); dispensar sem preencher ainda leva ao destino (correção do "beco sem saída" do legado, decisão do cliente); evento `whatsapp_modal_submit` disparado no envio | Nenhuma — decisão de produto já tomada e implementada |
| 37 | Extensão pós-24 | Deploy de teste em produção real (Vercel + domínio próprio) | ✅ Concluída (ver nota) | Issue 24 | Repositório Git dedicado criado; projeto Vercel `imediato-seguros` conectado ao GitHub; DNS via Cloudflare; site no ar com HTTPS em `comparaseguroonline.com.br` | Nenhuma — infraestrutura de teste operacional |
| 38 | Extensão pós-24 | Ajustes visuais pós-deploy (feedback ao vivo no domínio de teste) | ✅ Concluída (ver nota) | Issue 15, Issue 19 | Logos de seguradoras 2,5× maiores no desktop (`InsurersGrid`); ícone original do WhatsApp (`WhatsAppIcon`) em todos os pontos de contato; pulso no `WhatsAppFAB` | Nenhuma |
| 39 | Nova fase | Novo logotipo Imediato Seguros — Header, Footer, favicon, Open Graph | ✅ Concluída (ver nota) | `docs/BRAND_ASSETS.md` | Lockup completo conectado nos 4 usos; ícone recortado para favicon/OG; cartão branco no Footer/OG para contraste; Header adaptado (altura maior) para o lockup quase quadrado | Nenhuma — decisão de produto já tomada e implementada |
| 40 | Feedback registrado | Crítica à proposta de paleta/tipografia ("Projeto Visual.docx") | ⏳ Análise feita, aplicação adiada (decisão do cliente) | — | Achado de contraste WCAG AA no azul secundário (~3,7:1, abaixo do mínimo 4,5:1) | Decisão do cliente sobre adotar a nova paleta/tipografia (com o ajuste de contraste recomendado) |
| 41 | Extensão pós-24 | Paridade EspoCRM/Octadesk + backup Firebase + Cloud Function de reentrega (dev/UAT/produção) | ✅ Concluída (ver nota) | Issue 29, `docs/ANALISE_ESPOCRM_OCTADESK_FIREBASE_CLOUDRUN.md` | URL de EspoCRM resolvida automaticamente por ambiente; lead sempre gravado no Firebase (backup); Cloud Function reentrega o que falhar, testada de fato com sucesso | Fila de reentrega de longo prazo (Cloud Scheduler) para destinos fora do ar por muito tempo |
| 42 | Correção de bug | `lib/leads/store.ts` resiliente ao filesystem somente leitura da Vercel ("Não foi possível enviar agora") | ✅ Concluída (ver nota) | Issue 12 | `/api/lead` não retorna mais 500 por falha de gravação local; usa `/tmp` na Vercel; validado simulando `VERCEL=1` | Postgres real (solução definitiva, sem data prevista) |
| 43 | Correção de bug | Campo `DDD-CELULAR` malformado rejeitava todo lead no Octadesk desde o início da integração | ✅ Concluída (ver nota) | Issue 29 (Extensão #41) | Octadesk aceita o lead de primeira tentativa; corrigido no site e na Cloud Function; log de erro melhorado para diagnósticos futuros | — |
| 44 | Extensão pós-24 | Validação em tempo real (CPF/celular/e-mail) + captura em 2 fases no modal/formulário | ✅ Concluída (ver nota) | Issue 29, `docs/VALIDACAO_TEMPO_REAL_E_CAPTURA_2_FASES.md` | Checksum de CPF + celular estrito (`lib/validators.ts`); proxies server-side para APILayer/SafetyMails (`app/api/validate/*`); `stage: "initial"/"complete"` em `/api/lead` (contato inicial + atualização, sem lead duplicado); `ContactLeadModal`/`LeadForm` em 2 fases visuais; diálogo "Corrigir ou Prosseguir" no `LeadForm`; corrigido bug do EspoCRM exigir `NOME`/`Email` não-vazios e do parser de resposta não detectar `HTTP 200` com erro no corpo | SafetyMails permanece fora do ar (achado documentado, não específico deste site) — best-effort, nunca bloqueia |

---

> **Legenda de status:** ✅ Concluída · 🔄 Em execução · ⏳ Pendente.
> **Nota sobre a Issue 23 (✅ Concluída):** varredura real com axe-core (injetado via CDN no navegador) nas 5 páginas reais existentes (Home, `/cotacao`, `/seguro-auto`, `/obrigado`, `/alerta-de-fraude`) + checagem de overflow horizontal em 360/768/1440px + verificação de tamanho de todos os alvos de toque reais (`data-slot=button`, ícones sociais, hambúrguer, FAQ). 4 problemas reais encontrados e corrigidos:
> 1. Contraste do botão WhatsApp (3.18:1, reprovado) — `--color-whatsapp` escurecido de `#1aa564` para `#0e8449` (4.76:1).
> 2. `WhatsAppFAB`/`StickyCTA` fora de qualquer landmark — envolvidos em `role="complementary"`.
> 3. `<aside>` do `CotacaoTrustPanel` aninhado dentro de `<main>` (landmark `complementary` precisa ser top-level) — trocado por `<div>`.
> 4. Nenhum link "pular para o conteúdo" existia (WCAG 2.4.1, nível A) — adicionado em `app/layout.tsx`, testado com `CSS.forcePseudoState` (o ambiente de teste não tem foco real do SO, limitação já conhecida de issues anteriores).
>
> Teclado: Base UI (`Accordion`/`NavigationMenu`/`Drawer`) já cobre nativamente teclas de seta/Escape/Enter; não foi possível validar via automação nesta sessão pela mesma limitação de foco do ambiente — mitigado com verificação estática de semântica nativa (`<button>`/`<a>`) em todos os componentes.
>
> **Nota sobre a Issue 15 (✅ Concluída):** "Fora de escopo: criar componentes do zero (devem existir)" pressupunha que Hero, ComoFunciona, Benefits, CoverageCards e CTASection já existiriam de issues anteriores — nenhuma issue do backlog original os havia atribuído a ninguém. Por decisão explícita do usuário, foram construídos agora, como parte desta issue (reaproveitando `lib/company.ts`/`lib/ramos.ts` para todo dado). Três blocos do wireframe (seção 6.1) foram **deliberadamente omitidos** desta versão da Home, por decisão do usuário — dependem de assets/conteúdo que a auditoria confirmou existir no site atual mas que ainda não foram migrados/capturados (evitando placeholder ou dado inventado); cada um precisa de uma issue própria futura:
> - ~~**InsurersGrid**~~ — ✅ **Implementado em 2026-07-03** (ver nota da Extensão pós-24 "InsurersGrid" abaixo).
> - ~~**TeamStrip**~~ — ✅ **Implementado em 2026-07-03** (ver nota da Extensão pós-24 "TeamStrip" abaixo).
> - ~~**Testimonials**~~ — ✅ **Implementado em 2026-07-03** (ver nota da Extensão pós-24 "Testimonials" abaixo).
>
> **Nota sobre a Issue 16 (✅ Concluída):** desvio técnico necessário do caminho sugerido no plano (`app/(marketing)/seguro-[ramo]/page.tsx`) — confirmado que o App Router do Next.js **não suporta segmento dinâmico parcial** (pasta `seguro-[ramo]` é tratada como texto literal, não como prefixo + parâmetro). A rota real fica em `app/(marketing)/[ramoUrlSlug]/page.tsx` (segmento 100% dinâmico), com o prefixo `seguro-` conferido/removido em código — documentado em detalhe no próprio arquivo. Resultado final é idêntico ao pedido: 10 LPs (8 via `generateStaticParams` + `/fianca` e `/assistencia-24-horas` como rotas estáticas dedicadas, já que essas 2 não usam o prefixo `seguro-`). Reaproveita ao máximo os blocos da Issue 15 (`Hero`, `ComoFunciona`, `Benefits`, `CoverageCards`, `InsurersGrid`, `CTASection`, generalizados para aceitar `ramoSlug`) — sem cópia duplicada manual. **Omitidos por decisão consistente com a Issue 15** (mesmo motivo — dado não confirmado, não é dado inventado): Testimonials (mesmos assets não migrados) e **FAQ do ramo + objeções** — `ramo.faq[].answer`/`ramo.objections[].response` são literalmente `A_CONFIRMAR` para os 10 ramos (Issue 05); `ramo.arguments`, que É conteúdo real, foi renderizado normalmente. `InsurersGrid` foi adicionado também aqui em 2026-07-03 (ver nota abaixo).
>
> **Nota sobre a Issue 24 (⏳ Parcial):** feito o que é código puro: `next.config.mjs` agora tem `redirects()` com o único item **confirmado** (sem "?") da tabela de migração (`docs/INVENTARIO_URLS.md`, seção 59) — `/seguro-motos` (antigo, plural) → `/seguro-moto` (novo, singular), 301 — validado em runtime (`Invoke-WebRequest`, redirect 308/permanente confirmado) e com `npm run build` de produção limpo. Os demais itens da tabela são "manter" (mesma URL) ou estão marcados `?`/`TBD` (aguardando a auditoria real da seção 65 — Screaming Frog/Search Console/Google Ads, ainda não executada); adicionar redirects para eles seria inventar uma decisão de migração não confirmada, indo contra a regra explícita do próprio documento. **Bloqueado** (exige acesso externo que não tenho nesta sessão): configuração real do projeto na Vercel, variáveis de ambiente de produção (as 14 listadas em `lib/env.ts`/`.env.example` continuam com placeholder), e todo o checklist de go-live que depende de acesso a contas reais (GTM Preview, GA4 DebugView, teste de conversão no Google Ads, Search Console, DNS/SSL) — ver `docs/QA_CHECKLIST.md` seção 60 para a lista completa desses itens, nenhum deles executável localmente.
>
> **Nota sobre a Issue 18 (⏳ Parcial):** a parte de código foi feita — `scroll_depth`/`engaged_time` (únicos eventos do contrato da seção 20 que ainda não disparavam) agora são emitidos via `components/analytics/PageAnalytics.tsx`, validado no navegador. A parte que exige acesso real a GTM/GA4/Google Ads (validar no DebugView, mapear tags de conversão) **não pôde ser feita nesta sessão** — não há credenciais/acesso a essas contas. Ver `docs/DATA_LAYER_ATUAL.md` para o inventário do que existe hoje no site legado.
>
> **Nota sobre a Extensão pós-24 "Integrações externas do Webflow" (✅ Concluída, 2026-07-03):** rodada dedicada a mapear e implementar as integrações reais do ambiente DEV do Webflow, a partir do Head/Footer Code colado pelo usuário (`docs/WEBFLOW_CUSTOM_CODE_DEV.md`) e de decisões explícitas dele. Trabalho realizado:
> - **Documentação** (`docs/WEBFLOW_CUSTOM_CODE_DEV.md` novo; `INTEGRACOES_ATUAIS.md`, `DATA_LAYER_ATUAL.md`, `DADOS_OFICIAIS.md` atualizados) com os achados confirmados desta rodada.
> - **Variáveis de ambiente novas** (`.env.example`/`lib/env.ts`): `LEAD_ESPOCRM_WEBHOOK_URL`/`LEAD_OCTADESK_WEBHOOK_URL` (com as URLs reais dos proxies Cloud Run já confirmadas, substituindo `LEAD_WEBHOOK_URL`/`CRM_API_URL` como legado/opcional na lista de obrigatórias de produção), `NEXT_PUBLIC_RPA_ENABLED`/`NEXT_PUBLIC_RPA_API_BASE_URL`, `PH3A_ENRICHMENT_ENABLED`/`CPF_VALIDATE_PROXY_URL`.
> - **Múltiplos destinos de lead**: `lib/leads/espocrm.ts` e `lib/leads/octadesk.ts` (novos, compartilham retry via `lib/leads/proxy-sender.ts` e payload via `lib/leads/legacy-proxy-payload.ts`), chamados em paralelo por `lib/leads/webhook.ts`; `LeadRecord` estendido com `espocrmStatus`/`octadeskStatus`/tentativas por destino. Testado em mock mode (sem credenciais reais) — ver logs validados nesta sessão.
> - **RPA** (`lib/rpa.ts`, `components/lead/RPAProgressModal.tsx`): disparado após o `/api/lead` responder, não-bloqueante (tempo mínimo fixo de exibição antes do redirect, não o processo completo). Desabilitado por padrão (`NEXT_PUBLIC_RPA_ENABLED=false`).
> - **PH3A** (`lib/ph3a.ts`): enriquecimento de CPF server-side, não-bloqueante, disparado depois de persistir o lead. Simplificação deliberada: só atualiza o `LeadRecord` local, não reenvia atualização ao CRM (o legado tinha um fluxo separado de "criar"+"atualizar" no EspoCRM). Desabilitado por padrão, testado em mock mode (sem `CPF_VALIDATE_PROXY_URL` real).
> - **Banner de consentimento real** (`components/consent/ConsentBanner.tsx`): fecha a lacuna em que só existia o estado *default* "denied" — agora concede/nega os 4 sinais do Consent Mode v2 (`analytics_storage`, `ad_storage`, `ad_user_data`, `ad_personalization`), persiste a escolha em `localStorage` e reabre via o evento já existente `imediato:open-cookie-preferences`.
> - **SEO**: as 2 tags `google-site-verification` confirmadas no Head Code do Webflow foram adicionadas via `metadata.verification` em `app/layout.tsx`.
> - **Itens confirmados como não aplicáveis** (usuário): Mailchimp e CollectChat não funcionam mais — não implementados. CookieYes não será usado — mantido o Consent Mode v2 nativo.
> - **Validação**: `npm run typecheck`/`lint`/`check:hardcode`/`build` limpos; fluxo completo de `/api/lead` testado manualmente em mock mode (EspoCRM + Octadesk em paralelo, PH3A habilitado sem URL real, deduplicação) — ver `.data/leads.json` (git-ignored) durante o teste.
> - **Riscos documentados para validação em staging** (não executáveis nesta sessão, sem acesso a credenciais/ambiente real): (1) os proxies Cloud Run de EspoCRM/Octadesk não mostram nenhum header de autenticação no client-side — confirmar com TI se é o único mecanismo de proteção; (2) CORS do domínio `rpaimediatoseguros.com.br` para o domínio do novo site (chamada direta do navegador) — confirmar antes de habilitar `NEXT_PUBLIC_RPA_ENABLED` em produção; (3) testes que disparem o Octadesk atingem produção de verdade (sem ambiente de dev) — avaliar com o negócio se leads de teste precisam de marcação especial; (4) formato exato da resposta do PH3A via proxy não foi confirmado (só a função de transformação do lado do site legado foi lida) — parsing implementado de forma defensiva em `lib/ph3a.ts`.
>
> **Nota sobre a Extensão pós-24 "FraudAlert + DADOS_OFICIAIS + páginas legais" (✅ Concluída, 2026-07-03):**
> - `components/shared/FraudAlert.tsx` foi inserido em `app/(marketing)/layout.tsx` (acima do `Header`, fora de `(legal)`) — decisão de posição que estava pendente desde a Issue 22, agora que a Home (Issue 15) existe. Validado no navegador via CDP (texto, posição e dismiss funcionando).
> - O cliente confirmou, em sessão dedicada, **todos** os itens pendentes de `docs/DADOS_OFICIAIS.md`: nome fantasia, CNPJ, SUSEP, endereço, telefones (principal/emergência/ouvidoria), e-mail comercial, horário de atendimento (novo campo `business.hoursDisplay`/`hoursSchema` em `lib/company.ts`, agora também incluído no JSON-LD `InsuranceAgency` via `openingHours`), preços por ramo, e resolveu as 3 divergências que a auditoria do site legado havia identificado: anos de experiência (25, não 35+), nº de seguradoras parceiras (18, não 16) e % de satisfação (98%, não 96%). Qtd. de avaliações atualizada de "+2.000" para "+2.200" (valor mais recente informado pelo cliente). Todos os valores já aplicados em `lib/company.ts`.
> - **Política de Privacidade e Termos de Uso**: confirmado que não existia texto oficial do Jurídico para nenhum dos dois. A pedido explícito do cliente, foram redigidas versões genéricas (padrão de mercado para corretoras de seguros brasileiras + LGPD) e publicadas em `/politica-de-privacidade` e `/termos` — **adotadas como conteúdo oficial por decisão do próprio cliente**, não como uma revisão jurídica formal (registrado com essa ressalva em `docs/DADOS_OFICIAIS.md` para rastreabilidade).
> - O texto do Alerta de Fraude (rascunho já existente desde a Issue 22) foi aprovado pelo cliente como oficial, sem necessidade de nova redação.
>
> **Nota sobre a Extensão pós-24 "InsurersGrid — migração real dos 18 logos" (✅ Concluída, 2026-07-03):**
> - Diferente de GTM/GA4/Ads/Vercel (que exigem credenciais que não temos nesta sessão), os 18 logos de seguradoras (+ o logo principal da Imediato) são arquivos SVG **públicos**, hospedados no CDN do Webflow — já catalogados em `docs/BRAND_ASSETS.md` desde a Issue P-10. Foram baixados diretamente do site de produção (`https://www.segurosimediato.com.br`) e migrados para `/public/logos/seguradoras/` e `/public/logos/imediato-seguros.svg`.
> - **Achado técnico:** 10 dos 18 logos são vetores com cor cinza cravada diretamente no SVG (sem versão colorida). Os outros 8 (Loovi, Pier, Justos, Darwin, Usebens, Novo Seguros, Youse, Ezze) embutem uma imagem rasterizada com a cor **original** da marca — o site de produção usa `filter: grayscale(1)` via CSS para uniformizar visualmente os 18 como cinza (confirmado no CSS computado ao vivo). O componente `components/home/InsurersGrid.tsx` replica esse comportamento (`grayscale` → `hover:grayscale-0`), revelando cor de verdade nesses 8 logos ao passar o mouse — fiel ao "cinza → colorido no hover" da spec original.
> - **18º logo identificado:** o arquivo antes chamado "novo svg.svg" (não identificável pelo nome) foi confirmado como a seguradora real **Novo Seguros**, extraindo e visualizando a imagem rasterizada embutida no próprio SVG.
> - **Divergência de grafia resolvida:** arquivo de origem "justus svg" renomeado para `justos.svg`, seguindo a grafia oficial da marca ("Justos", seção 1.1 da especificação).
> - Novo arquivo `lib/seguradoras.ts` é a fonte de dados (nome + logo) para `InsurersGrid`, mantendo `company.business.insurersCount` (18, confirmado em `DADOS_OFICIAIS.md`) como a contagem oficial única.
> - `InsurersGrid` foi integrado na Home (após `Benefits`, espelhando a ordem do site legado) e nas 10 LPs de ramo (`RamoLandingPage`).
> - **Correção de consistência encontrada durante a validação:** ao confirmar 18 seguradoras, ficou evidente que `lib/ramos.ts` ainda tinha "comparando 16 seguradoras" hardcoded em 6 subheadlines e no título de SEO da LP de Auto — corrigido para "18" em todos os lugares (ver nota da Extensão pós-24 "FraudAlert + DADOS_OFICIAIS" acima para o restante dessa correção).
> - **Pendente/fora de escopo:** TeamStrip e Testimonials continuam omitidos (mesmos motivos documentados nas Issues 15/16). 4 dos 18 arquivos de logo (Darwin, Novo Seguros, Youse, Justos) embutem uma imagem rasterizada em base64 dentro do SVG, resultando em arquivos de 116–341KB (bem mais pesados que os demais, de 2–20KB) — funcionam corretamente, mas são candidatos a otimização futura (recriar como vetor real ou converter para um formato raster comprimido).
>
> **Nota sobre a Extensão pós-24 "TeamStrip + /equipe — migração real das 16 fotos" (✅ Concluída, 2026-07-03):**
> - Mesmo raciocínio do `InsurersGrid`: as 16 fotos da equipe são arquivos WebP **públicos**, hospedados no CDN do Webflow — já catalogadas em `docs/IMAGE_ASSETS_INVENTORY.md` desde a Issue P-10. Diferença importante: são fotos de **pessoas reais**, não ativos de marca — por isso a migração só ocorreu após o cliente confirmar explicitamente que os 16 colaboradores seguem na empresa e as fotos continuam atuais (perguntado antes de baixar/publicar, não presumido).
> - Fotos baixadas para `/public/team/`; `lib/team.ts` (novo) é a fonte de dados (nome + foto), mesmo padrão do site legado (só primeiro nome, sem cargo/função — não fornecido nesta rodada).
> - `components/home/TeamStrip.tsx` (novo) — resumo de 8 na Home, inserido após `CoverageCards` (ordem da seção 6.1 da especificação, pulando só `Testimonials`, ainda omitido).
> - `app/(marketing)/equipe/page.tsx` (novo) — grade completa com os 16, resolvendo um link do menu "A Imediato" que apontava para uma rota ainda não construída desde a Issue 06/07.
> - **Correção de dado observada nesta rodada**: a especificação (seção 6.1) usa "39 especialistas" como texto de exemplo do wireframe — **não é um dado confirmado**. O valor real usado em `team.length` é **16**, mesma contagem do próprio site de produção ("16 COLABORADORES PARA MELHOR ATENDÊ-LO") — nunca hardcoded como "39".
>
> **Nota sobre a Extensão pós-24 "Testimonials — carrossel Embla com avaliações reais" (✅ Concluída, 2026-07-03):**
> - Decisão do cliente: usar a API do Google Places como fonte primária das avaliações, mesmo sabendo de uma limitação real da API (Place Details retorna no máximo 5 avaliações por chamada, escolhidas pelo algoritmo do Google, sem controle manual sobre quais aparecem) e mesmo exigindo uma API key que não temos nesta sessão.
> - `lib/google-reviews.ts` (novo) implementa a chamada real à Places API (New), mas como não há `GOOGLE_PLACES_API_KEY`/`GOOGLE_PLACE_ID` configuradas, usa um fallback com **12 avaliações reais** — extraídas via inspeção do DOM (CDP) do widget de avaliações (Elfsight) já publicado no site de produção atual, que exibe avaliações reais do Google Meu Negócio. Não são depoimentos inventados, apenas uma fonte estática em vez de uma chamada de API ao vivo.
> - **Atenção para quando a API key chegar**: `company.google.placeId` (`lib/company.ts`) está no formato CID (`0x...:0x...`, usado em URLs do Google Maps) — a Places API exige o formato padrão de Place ID (geralmente iniciado em "ChIJ..."), que ainda não foi obtido/confirmado. É necessário descobrir o Place ID correto (ex.: via "Place ID Finder" do Google) antes de `GOOGLE_PLACE_ID` funcionar.
> - `components/home/Testimonials.tsx` (novo) — carrossel com `embla-carousel-react` + `embla-carousel-autoplay` (dependências instaladas nesta rodada, já aprovadas anteriormente pelo cliente), com arraste, setas prev/next, dots de navegação e auto-play pausável (`stopOnFocusIn`) — mesmas interações previstas na seção 29.3 da especificação.
> - Posicionado entre `CoverageCards` e `TeamStrip` na Home, seguindo a ordem da seção 6.1.
>
> **Nota sobre a Extensão pós-24 "QA/a11y focado nos blocos novos" (✅ Concluída, 2026-07-03):**
> - Auditoria com axe-core real (mesma metodologia da Issue 23) na Home (todos os blocos novos: `InsurersGrid`, `Testimonials`, `TeamStrip`) — **0 violações**. Em `/equipe` (página nova), 2 violações apareceram numa primeira tentativa (`document-title`, `html-has-lang`) — investigadas e confirmadas como um erro transitório `EBUSY` do OneDrive travando o `.next/server/webpack-runtime.js` durante a compilação da rota (mesma classe de problema já documentada em rodadas anteriores), não um bug de código; reiniciar o servidor resolveu, e a página carregou normalmente sem violações (confirmado via checagem manual: 0 imagens sem `alt`, 1 único `h1`, landmarks `main`/`nav` presentes, 0 botões sem rótulo acessível).
> - **1 problema real encontrado e corrigido**: os dots de navegação do `Testimonials` tinham apenas 8×8px de área de toque (bem abaixo do padrão de ≥44px já estabelecido no Design System desde a Issue 23). Corrigido envolvendo cada dot num `<button>` de 28×28px (mantendo o indicador visual pequeno como `<span>` interno) — ainda abaixo de 44px, mas uma melhoria significativa, aceitável para um indicador de paginação secundário já que a navegação primária (setas prev/next) cumpre 44×44px. Os dots também foram ocultados no mobile (`hidden sm:flex`) — com 12 avaliações, não cabem sem quebrar em telas de 360px; nesse tamanho a navegação por arraste + as setas prev/next (ambas mantidas) já cobrem a necessidade.
> - Confirmado sem overflow horizontal em 360px (`scrollWidth === clientWidth === 360`) em toda a Home, incluindo o mecanismo interno do carrossel Embla (que tem uma faixa `flex` mais larga que a viewport por padrão de funcionamento, mas contida por `overflow-hidden` no elemento pai — não é um vazamento visível real).
>
> **Nota de continuação (2026-07-07) — vetorização dos 8 logos concluída:** o freelancer contratado pelo cliente entregou os 8 arquivos vetorizados em `assets/Novos/`. Avaliação confirmou que são vetores puros de verdade (sem `<image>`/base64 embutido, feitos no Inkscape, cores da marca preservadas) — substituídos em `/public/logos/seguradoras/` (mesmo nome de arquivo, nenhuma mudança de código necessária). Validado com `typecheck`/`lint`/`check:hardcode`/`build` limpos e confirmação visual no navegador. Ver nota completa em `docs/BRAND_ASSETS.md`. Isso resolve o único item que ainda estava pendente na Issue 31.
>
> **Nota de continuação (2026-07-08) — API do Google Places ativada de verdade para o Testimonials:**
> - Cliente criou `GOOGLE_PLACES_API_KEY` no Google Cloud Console (projeto `leads-imediato-seguros` — mesmo projeto do Firebase de backup de leads legado), habilitou a **Places API (New)** e restringiu a chave só a essa API (sem restrição de aplicativo, já que a chamada é feita pelo servidor, não pelo navegador).
> - **Place ID correto encontrado por chamada real à própria API** (`places:searchText` buscando "Imediato Seguros" + endereço confirmado) — resultado bateu exatamente com nome, endereço e nota já confirmados (`ChIJAQAshElYzpQRJlHuOdPFrLE`). Não foi necessário usar a ferramenta manual "Place ID Finder" do Google.
> - **Risco confirmado na prática**: testando a chamada real, a API devolveu 5 avaliações "mais relevantes" segundo o algoritmo do Google, incluindo **uma reclamação de 1 estrela** sobre atendimento. Isso confirma exatamente a limitação avisada ao cliente antes de ativar a integração. **Decisão do cliente**: adicionar um filtro de nota mínima (`MIN_RATING_TO_SHOW = 4` em `lib/google-reviews.ts`) — descarta avaliações abaixo dessa nota, sem controle manual sobre quais avaliações específicas o Google escolhe mostrar entre as "mais relevantes".
> - `lib/google-reviews.ts` renomeado de `fetchGoogleReviews()` para `fetchGoogleReviewsSummary()` — agora retorna também a **nota média e a contagem total agregada reais** do Google (`rating`/`reviewCount`), buscadas na mesma chamada (`fieldMask: reviews,rating,userRatingCount`). Fallback (API indisponível): `company.business.googleRating`/`googleReviewsCount` (já confirmados pelo cliente).
> - `components/home/Testimonials.tsx` atualizado para exibir a nota/contagem reais no cabeçalho do carrossel (estrelas + "X,X baseado em Y avaliações no Google").
> - Validado com `typecheck`/`lint`/`check:hardcode`/`build` limpos e confirmação funcional via chamada HTTP direta ao servidor local (a ferramenta de navegador apresentou instabilidade nesta sessão — validação feita de forma alternativa, sem prejuízo à cobertura do teste).
>
> **Nota sobre a Extensão pós-24 "Rascunho de FAQ/objeções + coberturas por ramo" (✅ Concluída, 2026-07-08):**
> - A pedido do cliente ("pode redigir um rascunho genérico para eu revisar depois", mesmo tratamento dado a `/politica-de-privacidade`/`/termos`), escrevi respostas de FAQ/objeções para os 10 ramos em `lib/ramos.ts`, substituindo os placeholders `A_CONFIRMAR`. Textos propositalmente genéricos, sem números/condições comerciais específicas não confirmadas (franquia exata, prazo de carência em dias, percentual de desconto etc.) — quando a resposta dependeria de um dado real não confirmado, o texto direciona para "fale com um especialista" em vez de inventar o valor.
> - Também preenchi listas de coberturas (antes vazias) para os 9 ramos além de "auto", com nomes genéricos de mercado para cada tipo de veículo/segmento (não é a lista real de coberturas efetivamente oferecida pela Imediato).
> - **Diferença importante entre os dois**: o cliente pediu para as **coberturas** já aparecerem ao vivo nas LPs de ramo (via `CoverageCards`, que passou a renderizar em todas as 10 LPs, não só Auto) — mas pediu para **segurar FAQ/objeções** até revisar (continuam sem nenhuma renderização na LP, mesmo tratamento de antes, só que agora com o texto rascunho já escrito no código em vez de um placeholder vazio).
> - Nenhum dado comercial/regulatório real foi inventado — todo o conteúdo é genérico o suficiente para não comprometer a empresa caso publicado sem revisão adicional, mas está claramente marcado como RASCUNHO em comentário, ramo a ramo, para facilitar a revisão futura do time de Conteúdo/Comercial.
>
> **Nota sobre a Extensão pós-24 "`ContactLeadModal`" (✅ Concluída, 2026-07-08):**
> - Investigação anterior (`docs/LEGACY_JS_AUDIT.md`) confirmou que o site legado abre um modal de captura de lead (8 campos: DDD, Celular, Email, CEP, CPF, Placa, Ano do modelo, Marca/modelo) antes de redirecionar para WhatsApp/telefone, com um "beco sem saída" conhecido (fechar o modal sem preencher nunca leva ao destino). Perguntado explicitamente, o cliente decidiu **replicar o modal**, mas **corrigir o beco sem saída** (dispensar o modal sem preencher ainda leva ao WhatsApp/telefone) e aplicar em **todos os pontos de contato** do site (mesmo alcance do legado).
> - `leadSchema`/`LeadRecord`/payload dos proxies (EspoCRM/Octadesk) estendidos com `email`/`veiculoAno`/`veiculoMarcaModelo` — opcionais, só preenchidos por quem vem deste modal (o `LeadForm` multi-step da Issue 11 continua coletando só o mínimo).
> - **Reaproveitamento deliberado**: ao contrário do legado (que gravava direto no Firebase + EspoCRM + Octadesk a partir do próprio modal), o novo modal envia para o mesmo `/api/lead` já existente (Issue 12) — reaproveita idempotência, rate limit, retry, fallback de e-mail, sem duplicar lógica de integração.
> - `ContactModalContext`/`ContactModalProvider` (novo) — estado global via React Context, renderizado uma única vez em `app/layout.tsx`, para que os gatilhos (`WhatsAppButton`, `CallButton`, `WhatsAppFAB`, `StickyCTA`, links de telefone do `Footer`, extraídos para `FooterPhoneLink`) — espalhados por vários Client Components — compartilhem a mesma instância do modal.
> - Evento `whatsapp_modal_submit` (nome confirmado lendo o código do modal legado) adicionado a `lib/analytics.ts`, disparado no envio do modal, com `modal_channel: "whatsapp" | "phone"`.
> - Envio do lead ao `/api/lead` é não-bloqueante para a navegação: mesmo se a chamada falhar, o usuário ainda chega ao WhatsApp/telefone (nunca perder a conversão por uma falha de rede/servidor).
> - **Resolvido (2026-07-08)**: `WhatsAppButton` ganhou uma prop `skipModal` (restaura navegação direta, sem abrir o `ContactLeadModal`), usada só em `/obrigado` (`ObrigadoContent`) — o usuário acabou de preencher DDD/celular no `LeadForm` segundos antes; reabrir o modal ali seria fricção redundante. Todos os outros pontos de contato do site continuam abrindo o modal normalmente.
> - **URLs reais de EspoCRM/Octadesk resolvidas (2026-07-08)** — encontradas lendo o código-fonte real dos proxies Cloud Run e o script de deploy no diretório `imediatoseguros-rpa-playwright/WEBFLOW-SEGUROSIMEDIATO` (leitura, sem alterar nada lá), e configuradas em `.env.local`. Ver `docs/PROXIMOS_PASSOS.md`, seção 3.2.1, para as URLs exatas e o alerta importante sobre Octadesk de produção receber leads de teste.
>
> **Nota sobre as Extensões pós-24 #37/#38/#39 (2026-07-08):**
> - **#37 — Deploy de teste**: repositório Git criado do zero (o projeto não tinha um próprio até então), projeto Vercel conectado via integração Git (deploy automático a cada push), domínio de teste `comparaseguroonline.com.br` configurado via Cloudflare (DNS), certificado SSL emitido automaticamente pela Vercel. Corrigido um bug real em `lib/env.ts` durante o processo: o override manual `NEXT_PUBLIC_APP_ENV` não tinha prioridade de verdade sobre `VERCEL_ENV` (agora tem — necessário para classificar o deployment "Production" da Vercel como "staging" internamente, já que ainda faltam 10 variáveis de produção reais). Detalhes completos em `docs/PROXIMOS_PASSOS.md`, seções 3.1.1/3.1.2.
> - **#38 — Ajustes visuais**: feedback direto olhando o site de teste ao vivo. Logos de seguradoras (`InsurersGrid`) passaram por 2 rodadas de aumento (40px → 80px → 100px de altura no desktop, sempre mantendo 6 por linha). Ícone de WhatsApp trocado do genérico `MessageCircle` (lucide) para o glifo oficial da marca (`components/shared/WhatsAppIcon.tsx`, path público do Simple Icons) em todos os pontos de contato; `WhatsAppFAB` ganhou um anel pulsante (`animate-ping`, desligado em `prefers-reduced-motion`), replicando o comportamento do site legado.
> - **#39 — Logotipo no Header**: fase nova registrada a pedido do cliente, ainda sem implementação — aguardando o arquivo SVG que o cliente vai enviar. Encontrado (não usado ainda) um SVG do logotipo já migrado do Webflow em 2026-07-03 (`public/logos/imediato-seguros.svg`, confirmado vetor puro) — serve de referência, mas a versão do cliente tem prioridade quando chegar.
>
> **Nota sobre as Extensões #39/#40 (2026-07-08) — novo logotipo + crítica ao "Projeto Visual.docx":**
> - **#39 concluída**: o cliente enviou o arquivo definitivo (`Novo Logotipo Imediato Final SVG.svg`) — um lockup empilhado verticalmente (ícone "M" em gradiente azul + "IMEDIATO"/"SEGUROS" em duas linhas, texto desenhado em `<path>`, não HTML), proporção quase quadrada (≈0,93:1), não uma faixa larga como um logo de cabeçalho tradicional. Copiado para `public/logos/imediato-seguros-2026.svg` (substitui o `imediato-seguros.svg` de 2026-07-03, mantido no repositório sem uso ativo). Conectado a `Header` (logo-imagem no lugar do texto `company.tradeName`; altura do cabeçalho aumentada, `h-14`/`md:h-24` → `md:h-16` ao rolar, a pedido explícito do cliente para acomodar o lockup em tamanho legível) e `Footer` (mesmo arquivo, sobre um cartão branco). Extraído um recorte só do ícone "M" (`public/logos/imediato-seguros-icon.svg`, sem o texto) para `app/icon.svg` (favicon) e para a imagem Open Graph (`app/opengraph-image.tsx`, embutido como data URI base64) — exceção técnica necessária, não estética: favicons são sempre renderizados minúsculos (16–48px), o texto do lockup ficaria ilegível em qualquer tamanho.
> - **Achado de contraste resolvido com cartão branco**: o texto do logotipo é desenhado em `#003881`, quase idêntico ao fundo escuro já usado no `Footer`/gradiente da imagem Open Graph (`--color-brand-700: #0a2540`) — ficaria praticamente invisível direto sobre esses fundos. Resolvido com um cartão/pílula de fundo branco nos dois lugares, preservando as cores originais do arquivo. Decisão assumida por padrão (o cliente pulou a pergunta específica sobre isso) — fácil de revisar depois.
> - **#40 (crítica registrada, não implementada)**: a pedido do cliente, analisei o documento `Projeto Visual.docx` (nova proposta de paleta "Digital Clarity Blue" + tipografia "Digital Trust Typeface"). Direção da paleta é compatível com a atual, mas calculei que o azul "Secundária" (`#0088E8`) tem contraste de ~3,7:1 contra branco — abaixo do mínimo WCAG AA (4,5:1) — e o documento sugere esse tom no fundo de botões CTA com texto branco, o que reintroduziria o mesmo tipo de falha já corrigida uma vez neste projeto (botão do WhatsApp, Issue 23). Tipografia proposta troca Manrope por Poppins nos títulos (com caixa alta), mudança de personalidade visual perceptível — Inter no corpo do texto permanece igual. Cliente decidiu não aplicar agora, só registrar como feedback (`docs/PROXIMOS_PASSOS.md`, seção 3.8) para decisão futura.
>
> **Nota sobre a Extensão pós-24 #41 (✅ Concluída, 2026-07-12) — Paridade EspoCRM/Octadesk + Firebase + Cloud Function:**
> - Você pediu uma análise de como o site legado chama EspoCRM/Octadesk usando Firebase/Cloud Functions/Cloud Run (`docs/ANALISE_ESPOCRM_OCTADESK_FIREBASE_CLOUDRUN.md`) e depois pediu para reproduzir esse comportamento completo no site novo, respeitando dev/UAT/produção. Achado da análise: o modo "Firebase-Only" ativo em produção no legado depende de uma Cloud Function para entregar o lead, mas o próprio código carregado documenta que essa Cloud Function nunca foi implementada — uma contradição real, não confirmada como funcionando.
> - Decisão do cliente: replicar tudo (entrega direta + backup Firebase + Cloud Function própria), com um projeto Firebase **novo e dedicado** ao site novo (`imediato-seguros-site-novo`), não o `leads-imediato-seguros` do legado.
> - **Mapeamento de ambientes**: `LEAD_ESPOCRM_WEBHOOK_URL` (única) virou `LEAD_ESPOCRM_WEBHOOK_URL_DEV`/`_PROD`, resolvidas automaticamente por `appEnvironment` (`lib/env.ts`) em vez de depender de configuração manual por "tier" do Vercel — troca sozinho no dia do go-live real. UAT (`staging`) usa a mesma URL "dev". Octadesk continua uma única URL (sempre produção, sem ambiente de teste).
> - **Infraestrutura provisionada de ponta a ponta nesta sessão**: projeto Firebase, Realtime Database, faturamento (plano Blaze, mesma conta do legado), service account com permissão restrita (`roles/firebasedatabase.admin`), Cloud Function `retryLeadDelivery` (Node 22) implantada e com secrets configurados, política de limpeza de artefatos de build.
> - **Código**: `lib/leads/firebase-admin.ts` + `lib/leads/firebase-backup.ts` (backup não-bloqueante em `app/api/lead/route.ts`, Admin SDK — diferente do legado, que usa o SDK client-side); `firebase/functions/index.js` (Cloud Function, projeto Node.js separado do Next.js/Vercel, deploy manual via Firebase CLI, documentado em `firebase/README.md`).
> - **Validado com testes reais** (não só mock): lead de teste via `/api/lead` local gravou corretamente no Firebase; um registro manual simulando falha do EspoCRM foi corrigido pela Cloud Function em segundos, confirmado nos logs (`firebase functions:log`). `typecheck`/`check:hardcode` limpos.
>
> **Nota sobre a Correção de bug #42 (✅ Concluída, 2026-07-12) — `lib/leads/store.ts`:**
> - Diagnóstico de sessão anterior: você testou o envio de lead no site de teste e recebeu "Não foi possível enviar agora" — causa raiz era `lib/leads/store.ts` tentando escrever em `.data/leads.json` (caminho relativo ao diretório do projeto), mas o runtime serverless da Vercel só permite escrita em `/tmp`. A exceção subia sem tratamento até `app/api/lead/route.ts`, retornando 500 mesmo com o lead já entregue a EspoCRM/Octadesk.
> - Correção (aprovada como solução imediata, já que o Postgres real ainda não foi provisionado): `DATA_DIR` usa `os.tmpdir()` quando `process.env.VERCEL` está definido; `writeFile()` nunca mais lança exceção (falha só gera um aviso no log).
> - Validado de verdade simulando o ambiente da Vercel localmente (`VERCEL=1`) — lead de teste gravado corretamente no equivalente ao `/tmp`, sem erro.
> - Aceitável como solução best-effort (não definitiva) porque, desde a Extensão #41, todo lead também é gravado no Firebase (backup real e persistente, independente deste arquivo).
>
> **Nota sobre a Correção de bug #43 (✅ Concluída, 2026-07-12) — `DDD-CELULAR` malformado:**
> - Você pediu para investigar o achado paralelo registrado no item #41 (Octadesk rejeitando o payload de teste). Fiz uma chamada isolada e direta ao proxy Cloud Run do Octadesk, fora da aplicação, para capturar o corpo completo da resposta de erro (o código só registrava "falhou", sem o motivo — lacuna também corrigida).
> - **Causa raiz**: `lib/leads/legacy-proxy-payload.ts` montava `"DDD-CELULAR": "11-988887777"` (DDD + celular concatenados); o contrato real dos proxies espera só o DDD nesse campo (`"11"`) — confirmado lendo o código do modal legado (`MODAL_WHATSAPP_DEFINITIVO.js`) e validado com uma chamada real isolada: o mesmo payload, só trocando esse campo, passou de 500 "Telefone inválido" para 200 sucesso.
> - O EspoCRM tolera o formato errado (por isso sempre funcionou nos testes); só o Octadesk validava e rejeitava.
> - Corrigido nos dois lugares que tinham essa lógica: `lib/leads/legacy-proxy-payload.ts` (site) e `firebase/functions/index.js` (Cloud Function, tinha uma cópia). `lib/leads/proxy-sender.ts` também passou a logar o corpo da resposta em falhas — sem isso, o bug ficaria invisível de novo no futuro.
> - **Implicação importante**: esse bug existia desde o início da integração (2026-07-03) — nenhuma notificação por WhatsApp via Octadesk chegou a ser enviada de verdade até esta correção, embora o EspoCRM (CRM) sempre tenha recebido os leads normalmente.
>
> **Nota sobre a Extensão pós-24 #44 (✅ Concluída, 2026-07-13) — Validação em tempo real + captura em 2 fases:**
> - Você pediu uma análise profunda de como o modal (`MODAL_WHATSAPP_DEFINITIVO.js`) e o formulário principal (`webflow_injection_limpo.js`) do site legado fazem a captura em 2 fases e a validação em tempo real (CPF/celular/e-mail), e depois pediu para replicar isso no site novo, usando Firebase como no legado.
> - **Fase A**: checksum de CPF (`isValidCpf`, exportado de `lib/validators.ts`) e celular sempre com 9 dígitos começando em "9" — alinhado ao legado.
> - **Fase B**: proxies server-side `app/api/validate/phone`/`/email` — testei as credenciais reais do legado (fornecidas por você) diretamente: APILayer (telefone) funciona; SafetyMails (e-mail) não respondeu em nenhuma das 2 variantes conhecidas, um problema já documentado como não resolvido no próprio site legado. Implementado mesmo assim, best-effort (nunca bloqueia), a seu pedido explícito.
> - **Fase C**: `stage: "initial"/"complete"` em `/api/lead` — contato inicial (só telefone) cria o lead e atualização final referencia o mesmo `leadId`/`espocrmLeadId`, sem duplicar. Nessa investigação encontrei 2 bugs adicionais no fluxo já existente (não introduzidos por esta rodada, só descobertos ao testar o payload "inicial" pela primeira vez): o EspoCRM também exige o campo `NOME` não-vazio (além do `Email`, corrigido ontem) e nosso `proxy-sender.ts` não detectava esses erros porque o EspoCRM responde `HTTP 200` mesmo rejeitando o lead — corrigidos os dois, com testes reais contra o proxy Cloud Run confirmando `leadIdFlyingDonkeys`/`opportunityIdFlyingDonkeys` sendo capturados corretamente.
> - **Fase D/E**: `ContactLeadModal` e `LeadForm` reestruturados em 2 fases visuais/lógicas (etapa 1 dispara o contato inicial sem bloquear a navegação); diálogo "Corrigir ou Prosseguir" novo (`components/ui/alert-dialog.tsx`) no `LeadForm`, réplica do `SweetAlert` legado.
> - **Validado com testes reais de ponta a ponta** (não só mock): fluxo `initial` → `complete` testado contra o EspoCRM/Octadesk reais, confirmando um único lead atualizado (sem duplicar) e ambos os destinos recebendo os dados completos. `typecheck`/`build`/`check:hardcode` limpos.
> - Ver `docs/VALIDACAO_TEMPO_REAL_E_CAPTURA_2_FASES.md` para a arquitetura completa.
>
> Este backlog reflete a **ordem de execução recomendada (rev. 4.1)** — a numeração `#`/`ID` das issues de implementação preserva os números originais das 24 issues da especificação; a coluna `#` reflete apenas a ordem de execução, não uma renumeração.
> Para prompts completos, critérios de aceite detalhados, arquivos prováveis e passos de cada issue, consulte a seção 8 de `PLANO_IMPLEMENTACAO.md`.
> Nenhuma issue de fases 2+ (blog, CMS, pSEO, área do cliente, IA, integrações com seguradoras) foi incluída neste backlog.
